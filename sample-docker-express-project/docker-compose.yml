version: '2' # specify docker-compose version

# Define the services/containers to be run
services:
  express: #name of the second service
    build: . # specify the directory of the Dockerfile
    ports:
      - "5000:3000" #specify ports forwarding
